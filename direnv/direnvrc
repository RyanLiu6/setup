layout_uv() {
    if ! has uv; then
        log_error "uv not found. Please install uv first."
        return 1
    fi

    local venv=.venv
    if [[ ! -d $venv ]]; then
        log_status "creating python venv and installing dependencies"
        uv sync
    fi

    source "$venv/bin/activate"
}
