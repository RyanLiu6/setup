#!/bin/bash
set -e

SETUP_DIR="${SETUP_DIR:-$HOME/dev/setup}"

echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "Git Configuration Setup"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Check if git is installed
if ! command -v git &> /dev/null; then
    echo "⚠️  Git is not installed. Please install git first."
    exit 1
fi

echo "📝 Configuring git..."
echo ""

# Setup global gitignore
cp "$SETUP_DIR/git/.gitignore_global" ~/.gitignore_global
echo "  → Copied global gitignore to ~/.gitignore_global"

git config --global core.excludesfile ~/.gitignore_global
echo "  ✓ Configured git to use global gitignore"

git config --global push.autoSetupRemote true
echo "  ✓ Configured git push to auto-setup remote tracking"

echo ""
echo "✓ Git configuration complete!"
echo ""
